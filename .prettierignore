# https://prettier.io/docs/en/ignore.html#ignoring-files-prettierignore

# 全部的json文件 都默认不忽略 都要做格式化
!**/*.json

# skill 模板文件：内部使用分组注释划分插件区块，禁止 prettier 自动排版破坏结构
ai-plugins/**/templates/extensions.json

# 全部以tsconfig开头的 尾缀为json的配置文件 都被忽略
tsconfig*.json

# Skill Registry 由 release-ai-plugins 生成器以固定两空格/LF 输出，禁止提交钩子重排。
ai-plugins/skill-registry.json

# 无效 不生效的
**/snippets/*.md
