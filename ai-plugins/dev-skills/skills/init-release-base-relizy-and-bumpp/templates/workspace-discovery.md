# 仓库侦察结果（模板）

将以下字段填全后再进入决策与落盘。未知项写 `未知` 并标为阻塞项。

## 基础

| 字段                                | 值          |
| ----------------------------------- | ----------- |
| 包管理器                            | （如 pnpm） |
| 锁文件路径                          |             |
| 根 `package.json` 路径              |             |
| `pnpm-workspace.yaml`（或等价）路径 |             |
| 工作区 glob 列表                    |             |

## 已有发版工具

| 工具/文件                                      | 是否存在 | 备注 |
| ---------------------------------------------- | -------- | ---- |
| `changelog.config.ts` / `.cjs` / `.mjs`        |          |      |
| `relizy.config.ts`                             |          |      |
| Changesets / standard-version / changelogen 等 |          |      |

## 工作区包扫描（抽样 + 全量）

| 包路径 | `name` | `private` | 是否期望纳入 relizy |
| ------ | ------ | --------- | ------------------- |
|        |        |           |                     |

## Git 与 tag

| 项                                      | 说明 |
| --------------------------------------- | ---- |
| 当前默认分支                            |      |
| 是否已有 `@scope/pkg@x.y.z` 形式 tags   |      |
| 独立模式下各目标包是否具备 baseline tag |      |

## 环境与平台

| 项                                  | 值                          |
| ----------------------------------- | --------------------------- |
| 主要开发 OS                         | （Windows / macOS / Linux） |
| CI 镜像（若知）                     |                             |
| 是否已在目标环境跑过 relizy dry-run |                             |

## 文档与格式化

| 项                                         | 说明 |
| ------------------------------------------ | ---- |
| 根 README 是否已有发版章节                 |      |
| 根 `CHANGELOG.md` 是否存在                 |      |
| `formatCmd` 若存在，是否会触及 `README.md` |      |

## 侦察结论（一句话）

（例如：多应用 + 多包 workspace，要求独立版本，Windows 需兼容层，部分包 `private: true` 阻断扫描。）
