# `private` 与扫描范围

## 行为

- `private: true` 的 workspace 包**可能**不被 relizy 纳入扫描；以实际行为与文档为准。

## 技能要求

- 若要将某包纳入版本管理而需修改 `private`，必须：
  - 列出受影响包；
  - 取得明确确认；
  - 在 README 或破坏性变更报告中记录。

## 禁止

- 技能执行者**不得**在未确认时静默修改 `private`。
