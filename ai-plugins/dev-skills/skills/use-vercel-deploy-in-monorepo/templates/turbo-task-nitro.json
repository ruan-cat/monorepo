{
	"$comment": "适用形态 1 / 模式 A：Nitro 子包，产物搬运到 monorepo 根目录",
	"$schema": "https://turbo.build/schema.json",
	"extends": ["//"],
	"tasks": {
		"nitro:build:vercel": {
			"outputs": [".vercel/output/**"]
		},
		"move-vercel-output-to-root": {
			"dependsOn": ["nitro:build:vercel"],
			"outputs": [".vercel/output/**"]
		}
	}
}
