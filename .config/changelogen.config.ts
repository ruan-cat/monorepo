/**
 * Changelogen 配置
 * 使用 @ruan-cat/release-toolkit 提供的预设配置
 */
// 有疑惑 不认为需要这个工具 因为我们并不直接使用 changelogen 的 cli 功能。故用不到该配置
// 事实上废弃该文件。
import config from "@ruan-cat/release-toolkit/src/configs/changelogen.config.ts";

export default config;
