# claude-code-marketplace 插件商城的提示词

即 docs\prompts\claude-code-marketplace\index.md 的续期文件。记录更多历史提示词。

## 036 <!-- 已完成重构 --> 重构技能存储的目录层级，拓展成更加通用的技能分发方案

即 `docs\prompts\release-ai-plugins\rebuild-skills-release-way\index.md` 文件；

## 037 <!-- 已完成：init-prettier-git-hooks / init-vscode 增补 LF EOL 合并链路 --> 拓展 `init-prettier-git-hooks` 技能和 `init-vscode` 技能

我在多个项目内都遭遇到了很难受的 git 幽灵修改问题，我需要让 `init-prettier-git-hooks` 技能做好文件 eol 行尾字符的处理。

目前完整完善的处理链路如下：

1. `.gitattributes`：`* text=auto eol=lf`
2. `.editorconfig`：`end_of_line = lf`
3. `prettier.config.mjs`：`endOfLine: "lf"`（**禁止** `"auto"`，`"auto"` 在 Windows 上会保留/引入 CRLF）
4. `.vscode/settings.json`：`"files.eol": "\n"`
5. 运行 `git add --renormalize .` 实现 git 索引与工作区按 `.gitattributes` 归一化；**改完 `.gitattributes` 后不跑这一步会持续幽灵修改**（参见 [阮喵喵笔记 #025](https://notes.ruan-cat.com/bug/025-gitattributes-must-use-renormalize/)），若有暂存差异需单独提交规范化。

我需要让项目拥有这些能力，初始化配置时，同时确保处理了 git 的修改问题。重点统一 eol 配置为 `lf` 。

### `init-prettier-git-hooks` 技能的职责

#### 明确合并策略为合并策略

不仅仅是初始化配置文件了，而且还要实现配置文件的动态修改，覆盖，拓展，补全。以`init-prettier-git-hooks` 技能提供的模板为例子，去补充，改写目标项目的配置项。因为你需要处理的目标项目，很多本身都已经提供了 `.gitattributes` `.editorconfig` 和 `prettier.config.mjs` 这些配置文件，但是你要做到更新和拓展。所以你不应该再无条件覆盖了，而是动态识别与合并。

比如目标项目已经存在了 `prettier.config.mjs` 配置，但是 `endOfLine: "auto",` ，不符合我们现在的 `endOfLine: "lf",` 所以你应该用 lf 去精准覆盖掉 auto 配置。

#### 调研并准备一份合适的 `.gitattributes` 和 `.editorconfig` 配置

我需要你先调研本机电脑上其他项目是怎么配置编写 `.gitattributes` 和 `.editorconfig` 配置的，然后再确保能完成 eol 为 lf 的情况下，在 `ai-plugins\common-tools\skills\init-prettier-git-hooks\templates` 目录内补全模板。

按照严格的顺序，可调研的项目如下：

- D:\code\github-desktop-store\01s-11comm
- D:\code\github-desktop-store\gh.notes
- D:\code\github-desktop-store\gh.10wms
- D:\code\github-desktop-store\gh.drill-docx
- D:\code\github-desktop-store\gh.09oa
- D:\code\github-desktop-store\rm-monorepo

### `init-vscode` 技能的职责

专门负责 `.vscode/settings.json`：`"files.eol": "\n"` 的配置。`init-vscode` 技能专门实现 vscode 配置的处理，所以由 `init-vscode` 技能拓展这个配置。

## 038 <!-- 已完成： 修改sh打开的前缀的 bash 即可 --> 处理 cursor 无法识别读取 claude code 插件商城 sh 脚本并反复打开的问题

我在 cursor 每次实现对话时，每次都会自动打开 `C:\Users\pc.claude\plugins\cache\ruan-cat-tools\common-tools\3.2.0\scripts\user-prompt-logger.sh` 和 `C:\Users\pc.claude\plugins\cache\ruan-cat-tools\common-tools\3.2.0\scripts\task-complete-notifier.sh` 文件，我知道我在 `cursor` 内配置了读取是被 claude code 的插件商城，读取识别了全局的 hooks 并执行，但是我很不满意这个自动打开文件的做法。这导致页面不断的打开最新发布的 claude code 插件商城 sh 脚本，打断了我开发的心流状态。很影响我的开发状态；

是这个文件无法识别执行么？是里面的工作目录或者是环境变量写法，导致 `cursor` 无法执行 claude code 插件商城的 hooks sh 脚本么？

还是什么缘故？

我要修改的事 cursor 配置？还是去合理的修改这两个 .sh 的逻辑呢？

这两个 .sh 实际上来自于： `D:\code\github-desktop-store\gh.ruancat.monorepo\ai-plugins\common-tools\scripts` 项目提供的 sh 脚本。你看看是不是有写法不兼容的问题，导致 cursor 无法实现默认识别读取的故障？

另外，看一下截图，看看这个方案合适么？

![2026-03-29-20-12-36](https://gh-img-store.ruan-cat.com/img/2026-03-29-20-12-36.png)

## 039 <!-- 已完成 --> 处理 codex 在 `approval-requested` 事件内，对 `codex-notify-ccntf.ps1` 的处理能细节不合适的情况

如图，我每次在 codex vscode plugin 新开启一个对话，并且开始实现对话时，就出现以下情况。claude-notifier 调用失败了。显示的效果不好。

![2026-03-30-15-27-48](https://gh-img-store.ruan-cat.com/img/2026-03-30-15-27-48.png)

请你解决 `codex-notify-ccntf.ps1` 的处理细节。

---

去找全局的 codex 配置文件，并找到 C 盘全局的 `codex-notify-ccntf.ps1` 文件的处理细节。

## 040 <!-- 已完成 --> 将简单的 multica-ai/andrej-karpathy-skills 整合到 `ai-plugins\common-tools\skills\init-ai-md` 内

我需要拓展 init-ai-md 这款技能，初始化或再次手动执行 init-ai-md 技能时，我希望该技能能够将 `multica-ai/andrej-karpathy-skills` 的几个内容，添加到必要的 AI markdown 记忆文件内。

按照以下步骤获取并整合 `multica-ai/andrej-karpathy-skills` 的核心内容，整理成为 `ai-plugins\common-tools\skills\init-ai-md\templates\09.Karpathy Guidelines.md` 。

1. 阅读上下文：
   - 纯英文版的 skills： https://github.com/multica-ai/andrej-karpathy-skills/blob/main/skills/karpathy-guidelines/SKILL.md
   - 纯英文版理想化的 AI 记忆文件： https://github.com/multica-ai/andrej-karpathy-skills/blob/main/CLAUDE.md
   - andrej-karpathy-skills 的说明： https://github.com/multica-ai/andrej-karpathy-skills/blob/main/README.zh.md
2. 阅读 templates 的 markdown 文件格式。阅读 `ai-plugins\common-tools\skills\init-ai-md\templates` 目录内其他 markdown 文档的存储格式，语言，和标题组织方式（二级标题开头，三级标题分类，没有一级标题）。搞清楚新的 andrej-karpathy-skills 应该以何种方式组织好。
3. 编写内容。
   - 在 `ai-plugins\common-tools\skills\init-ai-md\templates\09.Karpathy Guidelines.md` 写入内容。
   - 用纯中文编写内容。不接受纯英文。做好技术类风格的翻译语调。

## 041 <!-- 已完成 --> 调研 `ai-plugins\common-tools\skills\do-long-task` 技能能否顺利完成 /goal 提示词生成能力（Claude Code /goal 与 Codex /goal）

我最近经常要求 AI 用 do-long-task 来生成 1500 字以内的长任务执行提示词。你可以去 memorix 看历史记录。但是我很疑惑的是，do-long-task 真的有办法专门来生成合适的长任务提示词么？

调研这个技能，并且给我增强，拓展这个技能的能力，让这个技能有能力去生成一个 1500 字以内的，能够按照`do-long-task`技能规范的提示词。

## 042 <!-- 正在WorkBuddy内执行 已完成 --> 设计 vercel 在 monorepo 场景下的部署 skills

我需要你制作一个通用的 skills，在 `ai-plugins\dev-skills\skills` 内新建一款名称为 `use-vercel-deploy-in-monorepo` 的技能，这个技能主要的职责是帮助用户在 monorepo 项目内，完成 vercel 项目部署。

你的参考具体项目如下：

- D:\code\ruan-cat\notes\docs\my-pull-requests\package.json nuxt 项目
- D:\code\ruan-cat\01s-11comm\apps\api\package.json nitro 接口项目
- D:\code\ruan-cat\01s-11comm\apps\admin\package.json admin+vue3 常规后台管理项目
- D:\code\ruan-cat\01s-11comm\apps\app\package.json uniapp H5 项目

你的核心方法论指导文章：

- https://juejin.cn/post/7610816257119354915

这个任务是一个复杂的任务，在探索上面，你要付出很大的努力完成多个工具的调用与探索。特别是对 vercel MCP 的使用。

如果你没办法使用 vercel MCP 来，并且使用这个 MCP 完成我的 vercel 项目配置的查看，那么请你及时和我沟通。

---

在 ai-plugins\dev-skills\skills\use-vercel-deploy-in-monorepo\SKILL.md 内，使用 move-vercel-output-to-root 脚本时，你没说清楚要额外安装依赖 `@ruan-cat/utils` ，move-vercel-output-to-root 命令是来自于 `@ruan-cat/utils` 依赖包的，你要在 skills 内说明清楚要安装的依赖，否则执行 move-vercel-output-to-root 命令会直接失败的。

## 043 <!-- WorkBuddy正在做 已完成 --> 调整 init-vscode 初始化的配置文件

针对 ai-plugins\common-tools\skills\init-vscode\templates\settings.json 。

```json
{
	"vue.server.includeLanguages": ["vue", "markdown"]
}
```

应该改成：

```json
{
	"vue.server.includeLanguages": ["vue"]
}
```

不要多配置 "markdown" 了，这个配置其实非常糟糕。并且我要求 init-vscode 技能和 ai-plugins\common-tools\skills\init-ai-md\SKILL.md 技能一样，拥有配置检查能力。在内部仔细检查清楚这个过时的配置，避免再出现不合适的配置。

至于本仓库内的 `.vscode\settings.json` ，不需要调整。

## 044 <!-- Kimi Work正在做 已完成 --> 增强 `init-simple-memorix` 技能的全局 hooks 处理能力

按照 D:\store\WorkBuddy\2026-6-30-common\2026-7-2-better-init-simple-memorx\better-init-simple-memorix-design.md 文档的要求，我要求你增强 `ai-plugins\common-tools\skills\init-simple-memorix\SKILL.md` 技能的能力。

## 045 <!-- WorkBuddy正在做 已完成 --> 新建一个通用工具形态的本地全局 agent 的 skills 同步器 skills

在 `ai-plugins\common-tools\skills` 内新建一个 skills 技能，实现对当前机器的全局本地 agent 工具的 skills 同步能力。名为 `sync-local-global-agents-skills` 。

设计目的是为了让本机大部分的 skills，都可以分发到大部分的本地 agent 工具内，都完成同步安装。

目前很多主流的 agent 工具，他们使用 vercel 提供的 skills node 包，都能够完成 skills 的批量安装，以及符号链接的生成与配置，实现当前机器内全局 skills 的唯一配置与分发。我目前的常用命令如下：

```bash
skills add https://github.com/ruan-cat/monorepo/tree/dev/ai-plugins/dev-skills/skills --skill '*' -g -y -a claude-code -a codex -a cursor -a antigravity -a trae -a qoder
skills add https://github.com/ruan-cat/monorepo/tree/dev/ai-plugins/common-tools/skills --skill '*' -g -y -a claude-code -a codex -a cursor -a antigravity -a trae -a qoder
```

大部分的本地 AI ide 和 AI cli，这些 agent 工具都能完成安装。但是仍旧有很多 agent 工具，仅仅依靠 skills 包是不能完成批量安装的。我还有很多本地 agent 工具，也需要完成 skill 的同步。

### 需要同步 skills 的 agent 平台的常见目录

来自 vercel skills 包全局存储的**唯一真理数据源**的 skills 目录： `C:\Users\pc\.agents\skills`

- WorkBuddy：
  `C:\Users\pc\.workbuddy\skills`
- QoderWork：
  `C:\Users\pc\.qoderworkcn\skills`
- Kimi Work：
  `C:\Users\pc\AppData\Roaming\kimi-desktop\daimon-share\daimon\skills`

其中，这里的 pc 是我的用户名，这个用户名是当前电脑的具体的用户名，你的处理逻辑应该要实现针对性的目录查找，不要写死具体的用户名。

### 未来随时可能会增加平台

我这里只是列举了我常用的，且 vercel skills 包没有覆盖到位的本地 agent 平台。未来大概率会持续性的增加这些平台。你要有随时增加平台的心理预期。

### 未来重复执行本技能的情况

这个 `sync-local-global-agents-skills` 技能并不是只执行了一次，比如我的全局 `C:\Users\pc\.agents\skills` 增加了新的 skills，那么我再次执行这个 skills 时，那么就要单纯的从 `C:\Users\pc\.agents\skills` 目录内获取并完成 skills 同步。就这么简单。你的处理逻辑做好识别，避免重复处理文件。

### 要求提供创建符号链接的 bash 批处理脚本完成全局 skills 的分发与同步

我要求 `sync-local-global-agents-skills` 要提供一个内部的，功能强悍的脚本，完成技能分发。

1. 我优先能够接受的是你写 typescript 脚本，用 tsx 来完成脚本运行。
2. 其次是用 node 直接运行 javascript 脚本。
3. 最差的是写 bash 或 cmd 这样的通用脚本完成任务。

### 在本项目内，为 `sync-local-global-agents-skills` 的脚本提供 vitest 测试用例

你不仅仅编写了 `sync-local-global-agents-skills` 的附属脚本，你还要在本 monorepo 项目内的 tests 目录内，模仿 tests\useIconEffect 的情况，编写测试用例。useIconEffect 是其他 skills 的附属代码，在本项目内也提供了严格而准确的 vitest 测试用例。

---

这个东西不要附带到我们的 skill 内。你宁可在本项目 monorepo 内的 tsconfig.json 处理这些脚本，也不要在 skills 内携带，这不对！
注册表的地址就是要硬编码，这是对的。
目录级符号链接，一定要是 lrwxrwxrwx 、MSYS=winsymlinks:native 的形式。备选才是 window 的 junction。

## 046 <!-- 已完成 Kimi Work正在做 --> 拓展 init-simple-memorix 技能对全局 memorix MCP 的配置写法

我需要 `ai-plugins\common-tools\skills\init-simple-memorix\SKILL.md` 技能增加一个新的能力，对目标机器的、本地 AI agent 工具的 MCP 配置文件，做维护与配置。

memorix 现在默认有办法自动识别可用的本地 agent，并安装 MCP，但是安装的配置格式往往是这样的：

以 json 格式的 MCP 配置为例子：

```json
{
	"mcpServers": {
		"memorix": {
			"command": "memorix",
			"args": ["serve"]
		}
	}
}
```

这个启动命令是 `memorix serve` 有个缺点就是无法暴露出全部完整的 memorix tools。这个启动模式是默认的 lite 模式，不是 memorix 的 full 模式。我期望 memorix MCP 的启动命令为 `memorix serve --mode full`，因此我需要你覆盖覆写 MCP 写法如下：

以 json 格式的 MCP 配置为例子：

```json
{
	"mcpServers": {
		"memorix": {
			"command": "memorix",
			"args": ["serve", "--mode", "full"]
		}
	}
}
```

关于此故障的参考资料： `D:\store\WorkBuddy\2026-6-30-common\2026-7-3-miss-memorix_promote-tools` 目录的 markdown 文档。

### 有效 MCP 配置文件位置参考

确定常见的本地 agent 的 MCP 配置位置。其中，这里的 pc 指的是用户的用户名。不是写死的硬编码地址，你应该动态的查找用户的全局 agent 配置。

这里的配置处理常见知名的 AI agent 工具以外，还有很多不常见的 AI agent 工具。诸如 WorkBuddy、Kimi Work、QoderWork 之类的。

- codex
  - `C:\Users\pc\.codex\config.toml`
  - `C:\Users\pc\.codex\config-2026-6-13-bg.toml`
- claude code
  - `C:\Users\pc\.claude.json`
- cursor
  - `C:\Users\pc\.cursor\mcp.json`
- WorkBuddy
  - `C:\Users\pc\.workbuddy\mcp.json`
  - `C:\Users\pc\.workbuddy\.mcp.json`
- zcode
  - `C:\Users\pc\.zcode\cli\config.json`
- vscode
  - `F:\store\vscode\user-data\User\mcp.json`
- qoder
  - `C:\Users\pc\AppData\Roaming\Qoder\SharedClientCache\mcp.json`
- kiro
  - `C:\Users\pc\.kiro\settings\mcp.json`

### 做好 MCP 位置清单随时增长的准备

未来一定会持续的拓展需要你维护的 MCP 文件。所以对于 init-simple-memorix 技能来说，要做好随时增加 MCP 安装位置清单表增加的准备。

关于这一部分，你的具体 skills 拓展实现，可以看看 `ai-plugins\common-tools\skills\sync-local-global-agents-skills\SKILL.md` 技能时怎么储备全局 agent 工具的 skills 目录清单的。模仿他的做法来做准备。

### 编写合适的脚本来完成定制 memorix MCP 的安装、改写任务

你应该模仿 `ai-plugins\common-tools\skills\sync-local-global-agents-skills\SKILL.md` 技能的做法，也编写合适的脚本，也编写 typescript 和兜底的脚本，完成 memorix MCP 的安装、改写任务。

- 如果目标文件已经有 memorix，那就增加参数，确保 memorix 启动的时候是 `memorix serve --mode full` 模式。
- 如果没有配置 memorix，那你就增加 memorix MCP 的配置。

### 补全必要的 vitest 配置

你应该也要模仿 `sync-local-global-agents-skills` 的做法，在本仓库内编写 vitest 测试用例。
注意看本仓库 AI 记忆文档内关于【对外分发 skill 的目录与路径约束】的章节，按照这里的规范来编写测试用例，别和之前 `sync-local-global-agents-skills` 一样犯错。

---

我要求你统一整理好 tests\init-simple-memorix 和 tests\sync-local-global-agents-skills 目录内的配置文件，我不希望你编写很多重复的，相同相似的 tsconfig.json 和 vitest.config.ts 配置。你设计并调整合适的配置文件存储和使用规范。并且去及时更新其他相关的说明 markdown 文档。

---

```txt
tests/
├── vitest.config.ts                      ← 公共 vitest 配置（新增）
├── init-simple-memorix/
│   ├── vitest.config.ts                  ← re-export 公共配置
│   ├── tsconfig.json                     ← 独立（include 路径不同）
│   └── install-mcp.test.ts
└── sync-local-global-agents-skills/
    ├── vitest.config.ts                  ← re-export 公共配置（已更新）
    ├── tsconfig.json                     ← 独立（include 路径不同）
    └── sync.test.ts
```

为什么还要设计那么多 tests\init-simple-memorix\vitest.config.ts 和 tests\sync-local-global-agents-skills\vitest.config.ts ？tsconfig.json 为什么不是单独一个，而是设计 2 个？以后增加更多的 skills，你岂不是做的更加差？做一大堆相似的东西？这叫输出垃圾！
还有，你到底有没有认真看【对外分发 skill 的目录与路径约束】这个东西呢？你看了 AI 记忆文档对这里的要求么？你怎么好意思写【> **测试目录规范**：本 skill 的测试用例统一存放在 `tests/sync-local-global-agents-skills/` 目录下，继承 `tests/vitest.config.ts` 公共 vitest 配置，避免在每个 skill 测试目录内重复定义相同的配置。】这样的东西呢？我们的 `ai-plugins\common-tools\skills\sync-local-global-agents-skills\SKILL.md` 和 `ai-plugins\common-tools\skills\init-simple-memorix\SKILL.md` 都是面向公共场景的通用 skills，为什么绑定关联本仓库的工作目录呢？这就完全不对！

## 047 <!-- 已完成 WorkBuddy正在做 --> 技能同步器 `sync-local-global-agents-skills` 额外有能力同步 memorix 的默认内部级别的 skills 文档

memorix 现在有能力提供 memorix 内部级别 skills。我希望`sync-local-global-agents-skills`有能力可以在当前机器的 vercel skills 包的全局默认 skills 的目录内，即 `C:\Users\pc\.agents\skills` 目录，直接补充实体的 memorix markdown 文件，并且去给其他 AI agent 工具同步分发 memorix 的 skills。

但是我需要想办法确保 memorix 官方的 skills 一定是最新的。我需要你给我想办法，去调研清楚 memorix cli 有没有能力直接输出内部预先写好的 skills markdown 文档。

我主要是担心以后执行 `sync-local-global-agents-skills` 技能后，没办法及时获取到最新的 memorix skills。我的备选设计方案如下：

1. 去找 memorix cli 有没有能力直接输出内部预写的 skills 文档。
2. 去找 memorix 官方仓库内固定写法的 skills 文档。

你也可以帮我设计更加合适的 markdown 获取方案。

### 编写专门的 memorix 脚本来处理 memorix 的 skills 获取与同步逻辑

我需要你专门搞一个合适的 memorix 脚本，来自动化的处理这些逻辑。便于以后执行本技能时，节省 token。

## 048 <!-- 已完成 codex正在做 --> 升级 init-simple-memorix 技能

根据以下内容：

- D:\store\WorkBuddy\2026-6-30-common\2026-7-6-fix-memorix-node-bug 目录内全部的文件
- D:\store\WorkBuddy\2026-6-30-common\memorix-setup-report.md

设计合适的计划，升级 `ai-plugins\common-tools\skills\init-simple-memorix\SKILL.md` 技能。

## 049 <!-- 已完成 Codex正在做 --> 升级增强 `use-vercel-deploy-in-monorepo` 技能

根据以下内容：

- D:\code\ruan-cat\01s-11comm\apps\admin\README.md 的 `vercel 云项目的部署配置` 章节
- D:\code\ruan-cat\01s-11comm\apps\app\README.md 的 `vercel 云项目的部署配置` 章节
- D:\code\ruan-cat\01s-11comm\apps\api\README.md 的 `vercel 云项目的部署配置` 章节
- `D:\code\ruan-cat\01s-11comm\.claude\skills\fix-bug\record-bug-fix-memory\2026-07-09-vercel-monorepo-root-config-pollution.md`
- `D:\code\ruan-cat\01s-11comm\apps\admin\src\docs\reports\2026-07-09-11comm-app-h5-vercel-root-config-incident.md`

我很怀疑我们的 `use-vercel-deploy-in-monorepo` 技能没有说清楚必要的内容。

设计合适的计划，升级更新 `ai-plugins\dev-skills\skills\use-vercel-deploy-in-monorepo\SKILL.md` 技能。

## 050 <!-- 已完成 Codex正在做 --> 升级 init-vscode 技能

阅读以下性能调研报告：

- `D:\store\WorkBuddy\2026-6-30-common\2026-7-9-fix-vscode-slow\2026-07-09-vscode-slow-report.md`
- `D:\store\WorkBuddy\2026-6-30-common\.agents\skills\fix-bug\record-bug-fix-memory\2026-07-09-vscode-slow-monorepo-startup.md`

设计合适的计划，升级更新 ai-plugins\common-tools\skills\init-vscode 技能。确保 init-vscode 技能学会忽略掉大多数常见的文件，加快 vscode 的访问性能。

## 051 <!-- 已完成； Codex正在做； 预期用Codex来完成 --> <!-- 已提供 需要在 11comm 项目内，多产生出足够多的报告文档，然后才能继续推进优化本任务 --> 升级迭代 do-long-task 技能

根据 `.agents\skills\skill-hardening-from-incidents` 技能的指导，阅读以下经验教训和事故报告：

- `D:\code\ruan-cat\01s-11comm\apps\admin\src\docs\reports\2026-07-11-do-long-task-openspec-doc-error.md`
- `D:\code\ruan-cat\01s-11comm\apps\admin\src\docs\reports\2026-07-11-openspec-do-longtask-self-reflection-report.md`

然后升级，迭代优化 `ai-plugins\common-tools\skills\do-long-task\SKILL.md` 技能。

1. 确保我们的技能以后能够学会精准准确的将长任务的过程报告文档、过程进度文档、和调研文档，新建到 openspec 任务内合适的子目录内，且附带正确的日期格式，确保不要出现散乱的 markdown 文件。
2. 确保我们的长任务技能，学会在执行的时候完整的使用 openspec 的一些列工件文件，而不是长期忽视掉这些文件：
   - 长任务技能从来不会去看具体业务项目内的 `.claude\skills` `.codex\skills` `.agents\skills` 目录内全部关于 openspec 的项目级别技能。
   - 长任务技能从来不去主动阅读、维护、迭代、拓展这些文件：`design.md` `proposal.md` `tasks.md` 、openspec 任务的 spec 规格文件来更新迭代标准，以及及时的拓展长任务的 `tasks.md` 任务进度表。
3. 长任务技能滥用 `agent-progress.md` 和 `agent-findings.md` 文档，导致实际文档长度过长，丢失核心进度标地。且长任务技能自己根本就没有合适的机制去清理、整理、压缩、标明当前长任务的**核心痛点**和**待办任务**。

## 052 <!-- 已完成 Codex正在做 --> 制作一个 use-pnpm 的技能

使用 `.agents\skills\skill-hardening-from-incidents\SKILL.md` 局部技能，阅读一系列事故报告和 markdown 文档，新建一个能够长期复用的技能。

在 `ai-plugins\common-tools\skills` 内新建一个技能。叫做 use-pnpm ，用来解决 pnpm 的一些问题。

根据 `D:\store\WorkBuddy\2026-6-30-common\2026-7-10-fix-pnpm` 目录内全部的文档，以及 `D:\store\WorkBuddy\2026-6-30-common\index.md` 的 `给出pnpm处理方案` 章节，新建一个简单的技能。这个技能目前主要的职责视为了实现 window 系统内 pnpm 的故障。

### 本技能做好未来随时拓展的准备

未来我随时会产生出更多的 pnpm 使用规范，以及 pnpm 排障的一揽子经验教训文档。未来这个技能要做好随时更新的准备。

## 053 <!-- 已完成 Codex正在做 --> 制作一个 agent team 执行后及时清理孤儿 node 进程的 skills

阅读以下内容：

- D:\store\WorkBuddy\2026-6-30-common\2026-7-9-fix-vscode-slow\2026-07-09-vscode-slow-report.md
- D:\store\WorkBuddy\2026-6-30-common\2026-7-8-learn-use-LangGraph-with-typescript\2026-07-11-langgraph-typescript-entry-research.md

在 ai-plugins\common-tools 目录内，新建一个技能来专门解决这个问题。以后我高强度调用 agent team 完成多个子代理开发时，就会要求使用这个工具完成孤儿 node 进程的批量清理。

## 054 <!-- 已完成 Codex正在做 --> 同时开发两个简易的工具安装技能，install-mcp 和 install-skills

- ai-plugins\common-tools\skills\install-mcp
- ai-plugins\common-tools\skills\install-skills

我需要你制作 install-mcp 和 install-skills 两个技能。

设计动机：

我经常需要高强度的安装 mcp 和 skills，但是我目前面对的 AI agent，不仅仅是常见的，还有很多不常见的，不算是很通用的 AI agent，这两个 skills 的目的就是为了实现常见和不常见的 AI agent 工具的配置同步。同步的准备好 MCP 和 skills 文件。

### 相关的参考资料

- ai-plugins\common-tools\skills\init-simple-memorix\SKILL.md
- ai-plugins\common-tools\skills\sync-local-global-agents-skills\SKILL.md

这两个技能都列举了非常清楚的清单，都告诉你要处理的目标冷门的，不常见与常见的 AI agent 的 skills 和 MCP 配置目录和文件。你应该要认真的看清楚这两个技能。

### 能力相互重复

我理解现在的 init-simple-memorix 和 sync-local-global-agents-skills ，在内部的能力上，会和 install-mcp install-skills 这两个新技能互有重叠，但是他们各有侧重。我要求 install-mcp install-skills 专门的解决这几个痛点问题：

1. 有哪些常用与不常用的 AI agent？列举好一个随时会拓展的清单，随时做好未来拓展清单的准备。
2. 这些 agent 工具的 skills 目录。和 MCP 配置文件的地址在哪里？
3. 安装 skills 的时候那些 agent 可以直接软链接？那些不一样，要做特殊处理。

install-mcp install-skills 两个技能解决的是有哪些对象？这些对象的位置在哪里？怎么批量安装？只负责统一的，通用的能力，更加倾向与清单职责。

### install-skills 承担核心的清单职责，sync-local-global-agents-skills 承担具体的安装任务

install-skills 和 sync-local-global-agents-skills 容易引起歧义，我要求这样的职责划分：

- install-skills 承担清单职责。
- sync-local-global-agents-skills 提供内部的可执行脚本，完成安装任务。

### install-mcp 本身也是一个清单

install-mcp 技能本身也是一个体现出用户使用倾向的安装清单。

### 该任务有不清晰和重复的风险情况

这个任务可能有很多重复，冗余，不干净，和其他 AI 执行后产生紊乱，未来不好统一维护清单，多个 skills 重复定义清单路径的情况。我要求你深度的去看看现有的 ai-plugins\common-tools\skills 和 ai-plugins\dev-skills\skills ，我希望用 install-skills 和 install-mcp 这两个技能承担清单职责，并且做好技能之间的相互调度。

重点用 grill-me 技能来协助我完成内容清晰化

## 055 <!-- 已完成 Codex正在做 --> 要求 `cleanup-agent-team-node-processes` 技能增强能力

我怀疑在我高强度使用子代理开发的时候，一次性子代理没有及时的关闭这些东西，导致每次使用 agent team 的时候，电脑的 CPU 和内存都非常紧张，我要求这个技能增强在 Codex 使用 agent team 的时候，专门去看看清空这些东西：

- node 进程
- npx.exe
- window 命令处理程序
- agent browser 浏览器进程
- agent browser cli

## 056 <!-- 已完成 Codex正在做 --> 修改 do-long-task 技能的存储方式

针对 ai-plugins\common-tools\skills\do-long-task\SKILL.md 。

我对 `agent-progress.md` 和 `agent-findings.md` 的存储位置很不满意。`agent-progress.md` 和 `agent-findings.md` 就应该要严格的位于 openspec 的 change 目录内，跟随着具体的 openspec 任务工件，我们绝对不要把好几个长任务的内容都累积到唯一的 `agent-progress.md` 和 `agent-findings.md` 内，那岂不是越来越臃肿了么？

1. 新建位置校对： do-long-task 技能必须要增加严格的筛选校对机制，确保新建长任务时，`agent-progress.md` 和 `agent-findings.md` 就应该要到具体的 openspec 任务目录内。
2. 错误位置警告： 你应该去检查 `agent-progress.md` 和 `agent-findings.md`，如果发现这两个文件有出现位置不对的，你应该要警告用户，并且去想办法迁移，移动到具体的 openspec 任务目录内。
   - 要么是移动到具体的 openspec/changes/ 内，作为正在执行的 openspec 任务的附属文件。
   - 要么是移动到 openspec\changes\archive 目录内作为已归档的任务附属文件。

## 057 <!-- 已完成 Codex正在做 --> 优化技能

根据 `docs\reports\2026-07-27-non-openai-agent-behavior-optimization.md` ，设计合适的方案，优化我们的 `ai-plugins` 目录下的一揽子技能，做好深入思考和调研设计

## 058 <!-- TODO: Codex正在做 --> 升级加固 init-shadcn-docs-nuxt 技能

我要求 ai-plugins\dev-skills\skills\init-shadcn-docs-nuxt\SKILL.md 技能拥有故障检修的能力。

我在两个项目都遇到很难受的 `shadcn-docs-nuxt` 系列的故障了，真的很难受。

阅读清楚这些关键的上下文：

- D:\code\ruan-cat\SmallAliceWeb\reports\2026-08-01-nuxt-content-monorepo-build-fragility-incident.md
- D:\code\ruan-cat\SmallAliceWeb\.agents\skills\fix-bug\record-bug-fix-memory\2026-08-01-nuxt-content-monorepo-compatibility.md
- D:\code\ruan-cat\eams-component-lib\.claude\skills\fix-bug\record-bug-fix-memory\SKILL.md
- 用 memorix 去查看 SmallAliceWeb 和 eams-component-lib 项目是在 nuxt content，shadcn-docs-nuxt，h3，这方面的关键词，找到难受的 nuxt content 构建失败的错误。

在得到这些上下文之后，你将这些错误经验，全部给 init-shadcn-docs-nuxt 技能做升级加固，确保这个技能以后去初始化一个全新的文档项目后，不会出现版本漂移，构建失败等莫名其妙的，脆弱的，不稳定的情况。我希望以后我的 monorepo 架构的 `shadcn-docs-nuxt` 包对应的 nuxt 文档项目，能够正常的完成 build。
这是一个很痛苦，很难受的故障，我要求你务必在 memorix 上充分的完成历史故障的调研，在 `D:\code\ruan-cat\eams-component-lib` 和 `D:\code\ruan-cat\SmallAliceWeb` 项目内，充分的完成历史错误调研。确保你清楚整个事故的来龙去脉。

---

你的删改修改范围实在是太大了，我很怀疑你的很多修改会导致技能本身的失忆情况

```txt
/**
	* node-server 产物不依赖构建期全站静态化。
	* 预渲染会拉起额外的 nitro-prerender 进程并加载完整 SSR 包，
	* 在默认 Node 堆限制（约 4GB）下容易 OOM。
	* 若需要纯静态托管，可改用 nuxi generate 或在有足够内存的环境执行。
	*/
crawlLinks: false,
},
hooks: {
"prerender:routes"(routes: Set<string>) {
	routes.clear();
},
},
```

这些信息都没必要了么？你是不是过拟合我提供的上下文了？我记得这个 `"prerender:routes"` 生命周期钩子在 D:\code\ruan-cat\eams-component-lib 项目内，是很重要的写法，你看看 `eams-component-lib` 为什么要保留这个钩子。以及你判断一下这个钩子有没有必要继续保留下去？

---

我怀疑你没有充分的把这个错误的历史教训记录到该技能内，这个技能很可能对错误的前因后果和排查方案不敏感。

---

SmallAliceWeb 与 eams-component-lib 两条事故线，你都有全面的将事故的前因后果都记录到这个技能内么？这个技能未来还要承担排查报错的和错误历史记忆的职责的。你确定做好对 init-shadcn-docs-nuxt 技能的增强加固了么？

## 059 <!-- TODO: --> 增强 install-skills 和 sync-local-global-agents-skills 技能

我要求这两个技能增加对 `C:\Users\pc\.codebuddy\skills` 目录的处理能力。使其可以同步 skills 文件到 codebuddy 内。CodeBuddy 是一个和 WorkBuddy 类似的 agent 工具，也需要实现同步 skills 的要求。
你的更改预期比较小，我记得这两个技能已经做好了未来随时增加 agent 平台的准备。
