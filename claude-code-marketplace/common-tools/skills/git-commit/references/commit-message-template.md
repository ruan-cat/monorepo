# 提交信息模板 (Conventional Commits)

```text
<type>(<scope>): <summary>

<变更内容>
<变更原因>
```

注意：

- summary 保持祈使句和具体化（"新增", "修复", "移除", "重构"）。
- 避免实现细节；专注于行为和意图。
- 如果是破坏性变更：在头部使用 `!` 和/或添加 `BREAKING CHANGE:`页脚。
