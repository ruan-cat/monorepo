---
"@ruan-cat/domains": patch
---

1. 调整 `11comm` 项目域名条目的 `order` 排列顺序，使 `ruan-cat.com` 域名条目优先展示（order 1、2），`ruancat6312.top` 域名条目靠后展示（order 3、4）。
2. 更新 `11comm` 项目中 `01s-11comm` 域名条目的 `description` 字段，将部署平台说明从 `cloudflare worker` 修改为 `vercel`，与实际部署情况保持一致。
